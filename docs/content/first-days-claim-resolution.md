# First Days Checklist: Claim Resolution

## Content Status
- All scenarios and steps in the `first-days-checklist` guide are currently marked with `status: 'VERIFIED'`.
- There are no `OWNER_REVIEW_REQUIRED` or `PROFESSIONAL_REVIEW_REQUIRED` claims.

## Review Schedule
- The guide is marked as `contentStatus: 'published'`.
- The `factCheckStatus` is `source-verified`.
- The `lastReviewed` date is set to `2026-08-05`.
- The `nextReview` date is set to `2027-02-05`.

## Governance Checks
- All steps within the `situations` arrays possess the required governance fields: `claimId`, `status`, and `reviewDate`.
- Any required `sourceId` mappings (e.g., ANAF contract registration, IGI residency processes) have been successfully linked to primary sources.
